import styles from './ArcCanvas.module.css'

export default function ArcCanvas() {
  return (
    <main id="arc-canvas" className={styles.canvas}>
      {/* ArcNav, ArcMetrics, ArcChart, ArcProjectList, ArcInput, ArcEmptyState — added in later tasks */}
    </main>
  )
}
